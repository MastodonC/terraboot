filebeat:
  prospectors:
    - input_type: stdin

multiline:
    pattern: '^[[:space:]]+|^Caused by:'
    negate: false
    match: after

fields:
    tag: '%{cluster-name}'

output:
  logstash:
    hosts: ["$${logstash-dns}:9200"]
