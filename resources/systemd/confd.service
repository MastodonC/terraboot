[Unit]
Description=running confd
After=install-confd.service
[Service]
ExecStart=/usr/local/bin/confd -daemon -backend zookeeper -log-level=debug -node http://${internal-lb-dns}:2181
ExecStop=/usr/bin/pkill confd